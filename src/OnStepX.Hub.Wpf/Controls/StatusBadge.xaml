<UserControl x:Class="ASCOM.OnStepX.Controls.StatusBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:ASCOM.OnStepX.Controls"
             Focusable="False">
  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
    <c:PulseDot Status="{Binding Status, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Pulsing="{Binding Pulsing, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Margin="0,0,6,0"/>
    <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
               Foreground="{Binding TextBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
               FontWeight="Medium"
               VerticalAlignment="Center"/>
  </StackPanel>
</UserControl>
